import React from 'react';
import styles from './Header.css';

const Header = () => {

  return (
    <header className={`${styles.Header}`}>
      <h1>Characters from Avatar: The Last Airbender</h1>
    </header >
  );
};

export default Header;
